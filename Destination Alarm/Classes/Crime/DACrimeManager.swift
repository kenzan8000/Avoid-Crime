import CoreLocation



/// MARK: - DACrimeManager
class DACrimeManager {

    /// MARK: - class method
    static let sharedInstance = DACrimeManager()

/*
    /// MARK: - public api
    func fetch(#location: CLLocation, radius: Double) -> [DACrime] {
        // from local DB
        let crimes = DACrime.fetch(location: location, radius: Double)
        if crimes.count > 0 { return crimes }

        // from SF Government server
//DACrime.save(#json: JSON, date: NSDate)
        return crimes
    }
*/
}
